<?php

// Backend ini API-only: seluruh halaman dilayani SPA React di folder `frontend/`.
// Berkas ini sengaja dibiarkan kosong karena masih ditunjuk bootstrap/app.php --
// grup middleware `web` (session + cookie CSRF) yang dipakai Sanctum berasal dari sini.
